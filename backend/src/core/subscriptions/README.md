# Core subscriptions

Shared platform ownership. Read-only subscription/access behavior is in users and permissions services. Verified billing writes are planned.

Core must not import business modules.
