# Core media

Shared platform ownership. Provider/service implementation is planned. No operational endpoint is registered for this capability.

Core must not import business modules.

`media.port.ts` defines the planned organization-scoped READY-media lookup boundary for Presence. It has no runtime adapter yet. See `docs/presence-backend-plan.md` for the required upload, processing, deletion and transaction semantics before exposing attachment APIs.
